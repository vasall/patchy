#include "patchy.h"



int main(int argc, char **argv)
{
        struct pa_document document;
        struct pa_string str;

        paInit(&document);


        paQuit(&document);
        return 0;
}
